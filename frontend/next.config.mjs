/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["picsum.photos", "64.media.tumblr.com", "i.pinimg.com"], // Allow images from both domains
    },
  };
  
  export default nextConfig;
  